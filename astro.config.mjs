// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    compressHTML: false,
    site: "https://project-planet-astro.netlify.app/"
});
